#include <iostream>
#include "functions.h"
int main() {
    cosnt int SIZE = 5;
    std::string namesArray[SIZE];
    int scoresArray[SIZE];
    int count = 0;

    addScore(namesArray, count, SIZE, "Mark", score 25);
    addScore(namesArray, count, SIZE, "Michael", score 30);
    addScore(namesArray, count, SIZE, "Matthew", score 35);
    addScore(namesArray, count, SIZE, "Niko", score 40);
    
    displayScores(namesArray, scoresArray, count);
    return 0;
}